{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/my_info_system",  // 你的可执行文件
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build",
        }
    ]
}
